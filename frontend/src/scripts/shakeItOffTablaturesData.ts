/**
 * Données des tablatures HTML/SVG pour "Shake It Off"
 * 
 * INSTRUCTIONS D'UTILISATION :
 * 
 * Option 1 - Utiliser ce fichier :
 * 1. Remplace les placeholders ci-dessous par les vraies tablatures HTML/SVG
 * 2. Exécute dans la console du navigateur : `await updateShakeItOffTablatures()`
 * 
 * Option 2 - Utiliser directement avec les tablatures en paramètres :
 * Exécute dans la console du navigateur :
 * ```
 * await updateShakeItOffTablaturesCustom(
 *   '<section id="tablature">...HTML guitare électrique...</section>',
 *   '<section id="tablature">...HTML basse...</section>',
 *   '<section id="tablature">...HTML piano...</section>'
 * )
 * ```
 * 
 * Le script mettra automatiquement à jour la leçon "La tablature" (partie 4)
 * du cours "Shake It Off" dans Supabase.
 */

export const shakeItOffTablatures = {
  electricGuitar: `<section id="tablature">
<!-- TODO: Colle ici le code HTML/SVG complet de la guitare électrique -->
</section>`,

  bass: `<section id="tablature">
<!-- TODO: Colle ici le code HTML/SVG complet de la basse -->
</section>`,

  piano: `<section id="tablature">
<!-- TODO: Colle ici le code HTML/SVG complet du piano -->
</section>`
}

